/**
 * Project-local loader shim so pi auto-discovers pi-dialer in this repo.
 * The real extension lives in ../../src/index.ts; edit there and /reload.
 */

export { default } from "../../src/index.ts";
